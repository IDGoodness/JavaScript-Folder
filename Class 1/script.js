
function add () {
    var firstInp = firstNum.value;
    var secondInp = secondNum.value;
    var total = Number(firstInp) + Number(secondInp);
    result.innerText = total;
}

function subt () {
    var firstInp = firstNum.value;
    var secondInp = secondNum.value;
    var total = Number(firstInp) - Number(secondInp);
    result.innerText = total;
}

function div() {
    var firstInp = firstNum.value;
    var secondInp = secondNum.value;
    var total = Number(firstInp) / Number(secondInp);
    result.innerText = total;
}

function mul() {
    var firstInp = firstNum.value;
    var secondInp = secondNum.value;
    var total = Number(firstInp) * Number(secondInp);
    result.innerText = total;
}
